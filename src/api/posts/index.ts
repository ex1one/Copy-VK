// const fetchPosts = (db: Firestore) => {
//   onSnapshot(collection(db, 'posts'), (doc) => {
//     doc.forEach((d) => [d.data()]);
//   });
// };
//
// export default fetchPosts; Либо удаляю, либо делаю через Redux
