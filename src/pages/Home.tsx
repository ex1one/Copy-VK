import React, { FC } from 'react';
import { Button } from '@mui/material';

const Home: FC = () => (
  <div>
    Home
    <Button variant="contained">Contained</Button>
  </div>
);

export default Home;
